pipeline {
    agent { 
        label 'nodejs' 
    }
    stages {
        stage('Install Shellcheck') {
            steps {
                sh 'sudo yum install -y epel-release'
                sh 'sudo yum install -y ShellCheck'  
            }
        }
    }
}